1. Paste the content of the enum. Do not include {} nor [], make sure to include the comments and the line breaks
2. run `node systemMessage.parser.js`
3. The JSON should be generated in the `messages.js` file